import ChunkFind from './ChunkFind';

export default {
  title: 'ChunkFind/ChunkFind',
  component: ChunkFind,
};

const Template = (args) => <ChunkFind {...args} />;

export const Default = Template.bind({});